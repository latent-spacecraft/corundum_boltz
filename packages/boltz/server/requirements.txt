# Minimal ligand-blob preprocessing env. No torch / biopython / boltz —
# the constraint extractors are vendored in scripts/python/ligand_blob.py and
# only need RDKit + numpy.
#
# Verified byte-equivalent to the boltz path on rdkit 2026.03.1 / numpy 1.26.4
# (a conda build). The pinned PyPI rdkit below is a real wheel; the distance
# bounds + SMARTS matching the blob depends on are stable across rdkit
# releases (only the stochastic ETKDG conformer coords vary, which the trunk
# re-augments anyway). Re-run scripts/python/verify_blob_equiv.py if you bump.
rdkit==2024.9.6
numpy==1.26.4
